# Network interface to sniff on (e.g. eth0, en0, any)
INTERFACE=any

# SQLite database path
DB_PATH=./data/metrics.db

# API server port
PORT=8000

# How often to flush sniffer stats to DB (seconds)
FLUSH_INTERVAL=5

# Rolling window for bytes/sec calculation (seconds)
ROLLING_WINDOW=10
